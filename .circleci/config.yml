# use CircleCI 2.0
version: 2
jobs:
  feature-express:
    working_directory: ~/repo
    docker:
      - image: circleci/node:4.8.2 # ...with this image as the primary container; this is where all `steps` will run
    steps:
      - checkout

      # - run:
      #     name: install-dependencies
      #     command: npm install

      - run: # run tests
          name: feature-express
          command: make build-feature-express